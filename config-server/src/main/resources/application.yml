server:
  port: 8888
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/jvictornascimento/microservice-configurations # diretorio publico meu de estudos
          default-label: main
          clone-on-start: true
